import React from "react";
import { shallow } from "enzyme";
import WithLogging from "./WithLogging";

describe("HOC WithLogging Tests", () => {
    it("Verify console.log calls on mount/unmount with pure HTML element", () => {
        
    });
})